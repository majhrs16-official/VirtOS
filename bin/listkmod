for kmod in $(find initrd/lib/modules/*/kernel/ -name "*.ko"); do
	deps=$(modinfo -F depends $kmod)
	if [ -z "$deps" ]; then
		echo "$(basename $kmod) not depends."

	else
		echo "$(basename $kmod) depends: $deps"
	fi
done
